Java Task
חלק 3
שאלה 1
1
GET /books
GET /books/{id}
POST/books
PUT /books/{id}
DELETE /books/{id}
GET /books/category/{categoryId}
GET /users
GET /users/{id}
POST /users
PUT /users/{id}
DELETE /users/{id}
2
 GET/books -מחזיר את כל הספרים
GET /books/{id} – מחזיר את הספר בעל הid
POST   /books – הוספת ספר חדש
PUT    /books/{id} – עדכון פרטי ספר
DELETE /books/{id} – מחיקת ספר 
GET /books/category/{categoryId} – חיפוש ספר לפי קטגוריה
  - GET /usersמחזיר את כל users
GET /users/{id} -מחזיר משתמש לפי id
POST /users- מוסיף  משתמש
   -PUT /users/{id}מעדכן את משתמש לפי id
  - DELETE /users/{id} מחיקת משתמש
 
3.
 public class book{
String id
String nameB
Int year
Boolean available 
String author 
public class User {
    private String name;    
    private String email;  
private List<String> borrowedBooks
}
4.
 String id 
String nameB -חובה
Int year -חובה
Boolean available 
String author - חובה
string name-חובה
 string email -חובה


שאלה 2
1.
1.	Order –  מייצגת הזמנה (פריטים, סכום, סטטוס).
2.	 Payment – מייצגת תשלום עבור הזמנה.
3.	OrderService – אחראית על יצירה, שליפה וניהול סטטוס של הזמנות.
4.	PaymentService – אחראית על ביצוע תשלומים ובדיקות תקינות.

2. 
אחריות של כל מחלקה:
Order (מחלקת הזמנה)
שומרת את פרטי ההזמנה: מזהה, רשימת פריטים, סכום כולל וסטטוס (NEW/PAID).
Payment (מחלקת תשלום)
שומרת פרטי תשלום: מזהה תשלום, מזהה הזמנה (orderId), סכום, אמצעי תשלום וסטטוס התשלום.
OrderService (טיפול בהזמנות)
אחראית ליצור הזמנות חדשות, לשלוף הזמנה לפי ID, ולעדכן את סטטוס ההזמנה (למשל ל–PAID לאחר תשלום).
PaymentService (טיפול בתשלומים)
אחראית לבצע תשלום בפועל:
•	לוודא שההזמנה קיימת
•	לוודא שהסכום שנשלם תואם לסכום ההזמנה
•	לעדכן את ההזמנה ל–PAID במקרה של תשלום תקין 
________________________________________
3.
1.	קישור נתונים:
לכל תשלום יש שדה orderId שמצביע על ההזמנה שאליה הוא שייך.
2.	בדיקת התאמה:
לפני ביצוע התשלום, המערכת שולפת את ההזמנה על פי orderId, ומוודאת:
o	שההזמנה קיימת
o	שהסכום לתשלום תואם לסכום ההזמנה
3.	ביצוע ועדכון:
אם כל הבדיקות תקינות, המערכת מבצעת את התשלום, מסמנת את התשלום כ-SUCCESS ומעדכנת את סטטוס ההזמנה ל–PAID.

